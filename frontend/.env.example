# Backend API base URL (NestJS backend, see techtians-backend/)
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# true = use local mock data / fake success when the backend is unreachable
NEXT_PUBLIC_USE_MOCK=false
